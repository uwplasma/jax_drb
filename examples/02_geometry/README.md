# Geometry

Scripts focused on geometry inputs/outputs and coefficient sanity checks.

- `tabulated_geometry_roundtrip.py`: export an analytic geometry to `.npz`, reload it with
  `TabulatedGeometry`, and verify that linear scans match.

